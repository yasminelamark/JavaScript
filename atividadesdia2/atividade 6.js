const leia = require('readline-sync')
const saldo = 10001

let nome, codigoColaborador , salario

codigoColaborador = leia.question('\nDigite o nome do colaboraror: ');
salario = leia.questionfloat('\nDigite o salario do colaborador: ')
codigoColaborador = leia.question('\nDigite o codigo de colaborador: ')

switch(codigoColaborador) {
    
        case valor1:
            
            break;
        case valor2:
            // Executa alguma ação caso esta condição seja verdadeira
            break;
        case valorN:
            // Executa alguma ação caso esta condição seja verdadeira
            break;
        default:
            // Opcional -  Executa alguma ação caso todas as condições acima forem falsas
    }
}

